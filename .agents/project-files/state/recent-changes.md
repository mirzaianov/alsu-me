# Recent Changes

Status: project-state recent implementation and documentation history

Keep only the 10 most recent entries.

## Entries

- 2026-05-26: Moved the canonical desktop breakpoint from `1061px` to `1161px`, documented the responsive ladder, aligned normal section `scroll-margin-top` with section vertical margins, added dockbar-aware controlled navigation offsets for upward/hash scrolling, and moved the `#hero` hash target to the true page top. [Reason why added: tablet layouts should persist through `1160px`, normal section anchors should land with predictable section spacing, upward navigation must account for the appearing dockbar, and `/#hero` should land at the top before hydration.]
- 2026-05-26: Refined button-like link states and marquee interaction behavior: hover/pressed styling now matches the shared button shape, touch tap highlights and text selection are suppressed on button controls, the testimonial `Read more` action keeps text-only hover styling, duplicate testimonial rows are clickable, and the testimonials/logo marquees pause for non-touch pointer and keyboard-focus interaction with a testimonial dialog-close pause guard. [Reason why added: visible controls inside moving marquees must remain clickable and stable while preserving touch behavior.]
- 2026-05-23: Added an extra-wide-only flex-growing Hero portrait lane, direct container-query-based fluid portrait sizing, a `1600px` extra-wide Hero footprint cap that accounts for the header and hero top margins, a larger profile-block gap, and updated the extra-wide image `sizes` hint. [Reason why added: the Hero portrait should use the row space left after the intro column and `64px` gap instead of resizing only at breakpoint steps, while very tall extra-wide screens need an upper height bound.]
- 2026-05-17: Aligned desktop and extra-wide Hero viewport math so the first screen ends at the viewport bottom, removed the desktop logo-strip bottom margin, and set the extra-wide hero-header top offset to `56px`. [Reason why added: wide screens need equal visible top/bottom hero edge spacing and must not reveal the About section in the first viewport.]
- 2026-05-17: Restored desktop and extra-wide Hero viewport-height filling with `max()` minimum-height floors and a flexing main content row while keeping CTA and logo rows fixed-size. [Reason why added: the Hero should fill wide screens without returning to uncontrolled gap stretching.]
- 2026-05-17: Added a Contact-only `1921px+` content-row and artwork scale with matching Next image `sizes`. [Reason why added: Contact was the remaining section image stuck at the tablet-sized desktop artwork while the rest of the extra-wide layout had been scaled.]
- 2026-05-17: Made the About content row stretch to the section width, added a badge-aware left photo lane, and increased the About portrait at desktop and extra-wide sizes with matching image `sizes`. [Reason why added: About was shrink-wrapping unlike sibling section rows, and the certificate badges need deliberate horizontal room beside the photo.]
- 2026-05-17: Changed Services cards to equal-height grid tracks sized by the largest card while keeping their current internal gap values and list spacer. [Reason why added: Services cards should share one visual height without returning to hard-coded card heights.]
- 2026-05-17: Changed Pricing tutor and translation cards to content-sized grid rows across breakpoints while preserving price, action, list, heading, and note slots. [Reason why added: fixed card heights were creating inconsistent extra space after padding and width adjustments.]
- 2026-05-16: Made the hero section content-sized at the desktop breakpoint, introduced a consistent `1921px+` token/container scale, adjusted hero/card/button dimensions and tutor-pricing action rows to match, changed extra-wide pricing cards to content-sized grid rows, increased heading-to-content gaps by standard spacing tokens, broadened extra-wide pricing cards while tightening their internal spacing, equalized the Contact email/Telegram button widths, and removed width caps from Services/Pricing `h3` subtitles. [Reason why added: large screens were stretching the hero vertically, and extra-large screens need systematic visual weight without changing ordinary desktop widths or constraining headings like body copy.]
