const Hero = () => {
  return (
    <section
      className={`
        mx-auto
        flex flex-col
        px-[var(--s)]
        tablet:px-[var(--xl)]
        desktop:px-[var(--3xl)]`}
    ></section>
  );
};

export default Hero;
